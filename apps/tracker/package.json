{
  "name": "@agent-analytics/tracker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/tracker.js",
  "scripts": {
    "build": "tsup src/index.ts --format iife --global-name AgentAnalytics --minify --out-dir dist",
    "dev": "tsup src/index.ts --format iife --global-name AgentAnalytics --out-dir dist --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agent-analytics/types": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.3.6",
    "typescript": "^5.7.3"
  }
}
